'use strict';
/*
 * @clarity/core, shared suite logic.
 *
 * Currently a placeholder. Populated across the roadmap:
 *   Phase 1, installer-engine (headless install/uninstall/launch + gogdl/legendary wrappers)
 *   Phase 2, the 29 shared IPC handlers (db, hltb, proton, sgdb, trailers,
 *             flatpak, achievements, settings, i18n) registered once
 */

module.exports = {};
